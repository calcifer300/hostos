node_modules
.next
.env.local
*.tsbuildinfo

.vercel
.env*

supabase/.temp

# Built by scripts/build-extension.mjs on every `npm run build`.
public/hostos-companion.zip

# Regenerated by next dev / next build, and flips between the two.
next-env.d.ts

# Vendored originals are reference material, not runtime. Archives, compiled
# Electron builds, nested clones and their dependencies never belong in git.
vendor/**/*.rar
vendor/**/*.7z
vendor/**/*.zip
vendor/**/dist/
vendor/**/node_modules/
vendor/**/.next/
vendor/legacy/
vendor/karl/archives/
